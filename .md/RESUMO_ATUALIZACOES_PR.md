# Resumo das atualizações

Este documento consolida as atualizações do projeto para facilitar a abertura de PR.

## Itens
- Estrutura principal do projeto em PHP organizada em `public/`, `actions/`, `config/` e `sql/`.
- Endpoints e ações para autenticação, hábitos e métricas.
- Arquivos de estilo para landing page e dashboard.
- Script de banco inicial em `sql/doitly.sql`.

## Objetivo deste commit
- Registrar um resumo único das atualizações no repositório para referência em pull requests.
